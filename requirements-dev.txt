# Local dev / evidence tooling only — NEVER installed into the deploy image
# (Dockerfile installs requirements.txt; keep it that way).
#
# Setup once per machine:
#   pip install -r requirements-dev.txt
#   python -m playwright install chromium
playwright>=1.45
uvicorn>=0.27
